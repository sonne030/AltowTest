import React from "react";
import Content from '../Content';
import Footer from '../Footer';
import Cards from '../Cards';

export const Home = () => {
  return (
    <div>
      <Content />
      <Cards />
      <Footer />
    </div>
  );
};
